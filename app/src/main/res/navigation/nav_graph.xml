<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/startViewFragment">
    <fragment
        android:id="@+id/input"
        android:name="com.afroditexekoukoulotaki.presentation.input.InputFragment"
        android:label="fragment_input"
        tools:layout="@layout/fragment_input" />
    <fragment
        android:id="@+id/startViewFragment"
        android:name="com.afroditexekoukoulotaki.presentation.startView.StartViewFragment"
        android:label="fragment_start_view"
        tools:layout="@layout/fragment_start_view" >
        <action
            android:id="@+id/action_startViewFragment_to_input"
            app:destination="@id/input" />
    </fragment>
</navigation>